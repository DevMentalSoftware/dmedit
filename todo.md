* Make our installer, and ensure "Edit in DMEdit" is added to the Windows Explorer context menu.* Check that defaults make sense for non-insiders* Selection inner corners no longer appear rounded* Find icons for all placeholders, possibly adding svg icon support.* Add all caps menu option like VS* Increase size of toolbar slightly* Move Recent to the tab bar as a dropdown.* Add Open to the tab bar next to the New button.* Add MD* Test large single line document.* Document.ConvertIndentation doesn't look efficient. It should not need the giant StringBuilder sb. * Search and Replace box text should "scroll" (not a scrollbar, but if I type in two 100 characters into a 50 wide box, then the caret should stay at the right with padding for the clear and dropdown, and leftmost characters are pushed off to the left. Home and End keys as well as SelectAll and other common shortcuts should work within the boxes.