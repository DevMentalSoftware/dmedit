* Make our installer, and ensure "Edit in DMEdit" is added to the Windows Explorer context menu.* Pick a different non-red color for when a files has mixed line endings, indent, or other.* Check that defaults make sense for non-insiders* Turn off wrapping should hide the wrap indicator* Need horizontal scrollbar when wrapping disabled if text doesn't fit* Need setting for selection color and intensity (default is pretty hard to see on laptop)* Selection at line end is hard to see, so it should extend to the right as if there is a single line ending character.* Selection inner corners no longer appear rounded* Find icons for all placeholders, possibly adding svg icon support.* Add all caps menu option like VS* Increase size of toolbar slightly* Move Recent to the tab bar as a dropdown.* Add Open to the tab bar next to the New button.* Add MD* Test large single line document.* 