* If one or more crash reports exist, then a Help menu item should be added to send the crash reports to support@devmental.com. Hide that menu item when the command is disabled (and add a SendCrashReport command if we don't have one yet). We may want a new Command boolean for whether HideWhenDisabled, so that these commands can be hidden when disabled instead of showing as greyed out in the menu.)* We need context menus in the editor, but need to decide what subset of commands should be there.* Add Help Menu items to open the company website (https://www.devmental.com) and github (https://github.com/DevMentalSoftware/dmedit)* Copy current default keymappings json to a new name, and change default mapping to be more "normal".* Change our Keyboard settings section to Commands, and add two new columns. 1) Checkbox for each row for whether to include in menus (maybe still be hidden if advanced), 2) Checkbox for each row for whether to include in the toolbar. Toolbar is the prime real estate, so by default we'll only put a few items there. Initial Toolbar can be (Undo, Redo, Cut, Copy, Paste, Delete, Wrap, and ShowWhitespace)* Actually implement the toolbar feature. Basically a centered area on the Menu Bar that automatically drops buttons that don't fit onto a submenu just as we do with tabs.* Check that our menu popup code supports overflow reasonably. If we don't have room to display all the dropdown menu items then it should wrap to a submenu automatically just as we do when we have too many tabs.* I don't trust that our profiles are accurate with the possible exception of Emacs. Try to find sources to verify all of our profile keyboard mappings. * Status bar separators should be thinner and span the height of the status bar. They should be the thickness and color of menu separators which we restyled. * 